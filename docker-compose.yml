version: '3'
services:
  #  autopus:
  #    container_name: autopus
  #    build:
  #      context: .
  #    ports:
  #      - "8080:8080"
  #    environment:
  #      AUTOPUS_POSTGRES_DATASOURCE: postgres://postgres:password@postgres:5432/autopus?sslmode=disable
  #    depends_on:
  #      postgres:
  #        condition: service_started
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"

  mysql:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: autopus
    ports:
      - "3306:3306"